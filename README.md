# zilliun.com

Static site for zilliun.com, served by GitHub Pages from the main branch.

- `index.html` is the whole site. Edit it and push to main; the change is live within a minute or two.
- `CNAME` binds the custom domain. Do not delete it.
